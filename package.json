{
    "name": "github-vite",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview --port 8080",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1",
        "vite": "^5.3.4"
    }
}
